async function refreshTokens(refreshToken: string) {
  // Do some refresh call to oauth provider and return new tokens

  return {
    accessToken: "new access token",
    refreshToken: "new refresh token",
  };
}
